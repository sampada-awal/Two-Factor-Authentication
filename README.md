# Two-Factor-Authentication
A Python-based 2FA system with TOTP (Google Authenticator), bcrypt-secured passwords, encrypted secret keys (Fernet), and a SQLite database for persistent user authentication.
